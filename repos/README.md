Full upstream repositories are not embedded in this bundle because the build runtime cannot establish outbound connections to GitHub.
Run `../scripts/fetch_upstreams.sh` or `../scripts/fetch_upstreams.ps1` from the bundle root on a normal internet-connected machine.
