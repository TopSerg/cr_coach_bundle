# API-fetched upstream snapshots

These are exact/near-exact text snapshots retrieved through the connected GitHub API on 2026-09-03, included because direct `git clone` was unavailable in the build environment.

- `cochon123-clash-royale-ai/.../parser.py` — SHA in upstream API: `40e97a728e4d6362602a45963ab0468c5039471d`.
- `cochon123-clash-royale-ai/.../hand_tracker.py` — upstream SHA: `af2c41e9e26f8bc6c17a134eeef810db0fa046b7`.

Note: `hand_tracker.py` in this bundle has been whitespace/format compacted while preserving logic to keep the bootstrap bundle manageable. Pull the repository for the authoritative upstream copy before modifying or redistributing it.
